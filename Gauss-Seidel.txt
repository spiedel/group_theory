#include <iosteam>
#include "ExampleGrid.h"
#include <iomanip>

int main(){

//define if circle or block
//enter positive potentials if any
//enter negative potentials if any
//enter ground potentials if any

grid U(x,y,dx,dy); // sophies way of implementing conditions into geometry - need to use const here for the potentials and grounds, fill all else with non const zeros

double par[x][y]; //this is the percentage of the difference in new iteration and the old iteraton to the iteration new iteration ie (Uold - Unew) / Unew
for (int i=0; i<x; i++){
	for (int j=0; j<y; j++ ) {
		par[i][j] = 100; //initialise all at 100%
	}
}

grid Uold
for (int i=0; i<x; i++){
	for (int j=0; j<y; j++ ) {
		if //U[i][j] defined as a potential or ground skip it
		else if par[i][j] =< 20 // 20 percent accuracy
			//do nothing
		else	
			
			U[i][j] = 	( U[i+1][j] + U[i-1][j] + U[i][j-1] + U[i][j+1] ) / 4
			par[i][j] = ( Unew[i][j] - Uold[i][j] ) / Unew[i][j]


	}
}
//







//end of file
}
